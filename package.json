{
  "name": "paretomed-evidencia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build/check-hechos-sync.js && node build/extract-data.js && npm run validate && npm run db:generate && node build/sync-src.js",
    "sync": "node build/sync-src.js",
    "dev": "npm run build && npm run serve",
    "serve": "cd dist && python -m http.server 8080",
    "validate": "node build/validate-case-integrity.js",
    "db:generate": "node src/scripts/generate-sql.js",
    "audit": "npm run build && node build/audit-dist.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0",
    "glob": "^13.0.6",
    "mammoth": "^1.12.0",
    "markdown-it": "^14.1.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "pdf-parse": "^2.4.5"
  }
}
